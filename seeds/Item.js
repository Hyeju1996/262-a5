let foods = ['Chicken', 'Pizza', 'remen']

console.log(foods.lenth)

// 2

let first = foods[0]
    // Chicken

let last = foods[foods.lengh - 1]
    // Pizza

foods.forEach(function(Item, index, arry));
console.log(Item, index) {)
    // Chicken 1
    // Pizza 2

let newlenghth = foods.push('Remen')
    // ["Chicken", "Pizza","Remen"];